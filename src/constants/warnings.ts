const WARNING_PREFIX: string = 'PULSAR WARNING:';

const COMPONENTS_SAME_UPDATE_PRIORITY = 
  `${WARNING_PREFIX} Two or more components have the same update priority. Components names: `;

export { COMPONENTS_SAME_UPDATE_PRIORITY };
